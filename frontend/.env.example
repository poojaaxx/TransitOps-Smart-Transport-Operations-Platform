# Only needed if you serve the frontend and backend from different origins
# (e.g. separate deploys). In local dev, the Vite dev server proxies /api
# and /uploads to the backend automatically (see vite.config.js), so this
# can stay unset.
VITE_API_BASE_URL=
